spring:
  application:
    name: agentdesk

  datasource:
    url: jdbc:postgresql://localhost:5432/agentdesk
    username: pratikbehera
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    org:
      springframework:
        security=DEBUG: